name: lesson_package_repository
description: "Dart package which manages the lesson packages."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.15.1
  equatable: ^2.0.7
  cloud_firestore: ^5.6.11

  customer_repository:
    path: ../customer_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
